/target
/build

# Vendored embedding model (~31 MB) — fetch with `make model`
/models
src/embedding/potion-retrieval-32M

# Corpus fetched by `make seed`, and the storage it is scanned into. Neither is
# ours to commit: the corpus is somebody else's repository and the storage is a
# few hundred MB of derived vectors.
/seed
/home

# Local store
/data
*.db
*.db-journal
*.db-wal
*.lance
