/target
/build
/oldbuild

# Corpus fetched by `make seed`, and the storage it is scanned into. Neither is
# ours to commit: the corpus is somebody else's repository and the storage is
# derived from it.
/seed
/home
/oldhome
# Local store
/data
*.db
*.db-journal
*.db-wal
